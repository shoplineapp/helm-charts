customRole:
  - name: shopline-tmplate1
    rules:
      - apiGroups: [""]
        resources: ["pods", "pods/log"]
        verbs: ["get", "watch", "list"]
      - apiGroups: [""]
        resources: ["pods/exec"]
        verbs: ["create"]

  - name: shopline-tmplate2
    rules:
      - apiGroups: [""]
        resources: ["pods", "pods/log"]
        verbs: ["get", "watch", "list"]
